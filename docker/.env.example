# Database
POSTGRES_PASSWORD=postgres

# JWT (change in production!)
JWT_SECRET=super-secret-key-change-in-production

# CV Engine
MODEL_PATH=/app/models/box_model.pt

# NVR (optional — can also configure via UI)
NVR_HOST=
NVR_USER=uxdp
NVR_PASS=cw8adc
